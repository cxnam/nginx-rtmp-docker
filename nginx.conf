daemon off;

error_log /dev/stdout info;

events {
    worker_connections 1024;
}

rtmp {
    server {
        listen ${RTMP_PORT};
        chunk_size 4000;

        application stream {
            live on;

	        # exec ffmpeg -i rtmp://localhost/stream/hello -r 30 -ar 44100 -s 1280x720 -c:a libfdk_aac -b:a 90k -movflags +faststart -preset veryfast -crf 28 -tune zerolatency -profile:v baseline -maxrate 1000k -vcodec libx264 -bufsize 10000k -g 60 -max_muxing_queue_size 1024 -f flv rtmp://entrypoint.sabai.vn/1b22c0ba/uiza-test1;

            exec ffmpeg -i rtmp://localhost/stream/$name  -r 30 -ar 44100 -s 1280x720 -c:a libfdk_aac -b:a 90k -movflags +faststart -preset veryfast -crf 28 -tune zerolatency -profile:v baseline -maxrate 1000k -vcodec libx264 -bufsize 10000k -g 60 -max_muxing_queue_size 1024 -f flv rtmp://entrypoint.sabai.vn/1b22c0ba/uiza-test1;

            # exec ffmpeg -i rtmp://localhost:1935/stream/$name
            #  -c:a libfdk_aac -b:a 128k -c:v libx264 -b:v 2500k -f flv -g 30 -r 30 -s 1280x720 -preset superfast -profile:v baseline rtmp://localhost:1935/hls/$name_720p2628kbs
            #  -c:a libfdk_aac -b:a 128k -c:v libx264 -b:v 1000k -f flv -g 30 -r 30 -s 854x480 -preset superfast -profile:v baseline rtmp://localhost:1935/hls/$name_480p1128kbs
            #  -c:a libfdk_aac -b:a 128k -c:v libx264 -b:v 750k -f flv -g 30 -r 30 -s 640x360 -preset superfast -profile:v baseline rtmp://localhost:1935/hls/$name_360p878kbs
            #  -c:a libfdk_aac -b:a 128k -c:v libx264 -b:v 400k -f flv -g 30 -r 30 -s 426x240 -preset superfast -profile:v baseline rtmp://localhost:1935/hls/$name_240p528kbs
            #  -c:a libfdk_aac -b:a 64k -c:v libx264 -b:v 200k -f flv -g 15 -r 15 -s 426x240 -preset superfast -profile:v baseline rtmp://localhost:1935/hls/$name_240p264kbs;
        }

    }
}

http {
    access_log /dev/stdout combined;

    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_protocols       TLSv1 TLSv1.1 TLSv1.2;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    server {
        listen ${HTTP_PORT};

        location /stat {
            rtmp_stat all;
            rtmp_stat_stylesheet static/stat.xsl;
        }

        location /static {
            alias /www/static;
        }

        location = /crossdomain.xml {
            root /www/static;
            default_type text/xml;
            expires 24h;
        }
    }
}

